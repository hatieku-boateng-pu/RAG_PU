name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install flake8 black pytest pytest-cov
    
    - name: Lint with flake8
      run: |
        # Stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=.venv,.git,__pycache__
        # Exit-zero treats all errors as warnings. Check for common code quality issues
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics --exclude=.venv,.git,__pycache__
    
    - name: Check code formatting with black
      run: |
        black --check --diff --exclude="/(\.venv|\.git|__pycache__)/" .
      continue-on-error: true
    
    - name: Run tests
      run: |
        # Run pytest if tests exist, excluding virtual environments
        if [ -d "tests" ] || find . -path "./.venv" -prune -o -path "./.git" -prune -o -name "test_*.py" -print | grep -q .; then
          pytest --cov=. --cov-report=xml --cov-report=term
        else
          echo "No tests found, skipping test execution"
        fi
      continue-on-error: true
    
    - name: Upload coverage reports
      if: matrix.python-version == '3.11'
      uses: codecov/codecov-action@v4
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false
      continue-on-error: true
