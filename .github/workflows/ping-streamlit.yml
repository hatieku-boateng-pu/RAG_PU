name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl -I https://97budq2zikgtwxoqgmpbzb.streamlit.app/
