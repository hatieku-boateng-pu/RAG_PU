name: Keep PU Statute App Alive

on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping PU Statute App
        run: |
          curl -I https://pu-statute.streamlit.app/
